[project]
name = "tftp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiofiles>=24.1.0",
    "async-lru>=2.0.5",
    "asyncio>=3.4.3",
    "lru-cache>=0.2.3",
]
